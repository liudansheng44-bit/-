# 截骨大师（Osteotomy Master）原型

这是一个纯前端的演示版 app，用于：

- 根据当前/目标 HKA 计算理论矫正角度。
- 根据铰链距离估算开口楔形间隙。
- 可选根据截骨面长度估算远端位移。
- 使用 `localStorage` 做简易随访记录。

## 运行方式

直接用浏览器打开 `index.html`，或者执行：

```bash
python3 -m http.server 8080
```

然后访问 `http://localhost:8080`。

## 免责声明

本项目仅用于教学与流程演示，**不能替代**专业医疗判断与手术计划。
